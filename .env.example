# Database Configuration
# Separate credentials for better security
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=chatdb
DATABASE_USER=postgres
DATABASE_PASSWORD=your-secure-database-password-here

# Security Keys (REQUIRED - Generate strong random keys)
# Generate SECRET_KEY with: openssl rand -hex 32
# Generate ENCRYPTION_KEY with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
SECRET_KEY=your-secret-key-here-use-openssl-rand-hex-32
ENCRYPTION_KEY=your-encryption-key-here-use-fernet-generate-key

# CORS Configuration
# Comma-separated list of allowed origins (no spaces)
# For development: Include both localhost and 127.0.0.1 (Windows compatibility)
# For production: https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:3000,http://127.0.0.1:8080

# JWT Configuration (Optional - defaults provided)
# ACCESS_TOKEN_EXPIRE_MINUTES=30

# Quick Start for Development:
# 1. Copy this file to .env: cp .env.example .env
# 2. Generate SECRET_KEY: openssl rand -hex 32
# 3. Generate ENCRYPTION_KEY: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# 4. Replace the placeholder values above with your generated keys
# 5. Update database credentials if needed
# 6. Start Docker: docker-compose up -d
# 7. Run migrations: alembic upgrade head
# 8. Start server: python -m uvicorn app.main:app --reload
